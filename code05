#include <stdio.h>

int main() {
    int i,n,sum=0,b;
    printf("enter n ");
	 scanf("%d",&n);
	 int a[n-1];
	 for(i=0;i<n-1;i++){
	     scanf("%d",&a[i]);
	     sum=sum+a[i];
	 }
	 b=(n*(n+1))/2-sum;
	 printf("missing value=%d",b);
    return 0;
}
